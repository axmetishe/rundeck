getent group ${packagingAppGroup} >/dev/null || groupadd ${packagingAppGroup}
getent passwd ${packagingAppUser} >/dev/null || useradd -d ${packagingAppPrefix} -m -g ${packagingAppGroup} ${packagingAppUser}
